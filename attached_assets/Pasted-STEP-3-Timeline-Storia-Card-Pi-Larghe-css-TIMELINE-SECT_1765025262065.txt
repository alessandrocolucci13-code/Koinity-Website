STEP 3: Timeline Storia - Card Più Larghe
css/* === TIMELINE SECTION === */
.storia-section {
  padding: 100px 40px;
  max-width: 1300px;
  margin: 0 auto;
}

.storia-title {
  font-size: 48px;
  font-weight: 900;
  color: #fff;
  text-align: center;
  margin-bottom: 16px;
}

.storia-title .highlight {
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.storia-subtitle {
  font-size: 20px;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.8);
  text-align: center;
  margin-bottom: 64px;
}

/* Timeline Grid - PIÙ LARGO */
.timeline-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 40px;
  position: relative;
}

/* Linea connettrice */
.timeline-grid::before {
  content: '';
  position: absolute;
  top: 80px;
  left: 10%;
  right: 10%;
  height: 2px;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255, 214, 0, 0.5) 20%,
    rgba(255, 214, 0, 0.5) 80%,
    transparent 100%
  );
  z-index: 0;
}

/* Timeline Card */
.timeline-card {
  position: relative;
  padding: 48px 32px;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.06) 0%,
    rgba(255, 214, 0, 0.03) 100%
  );
  border: 2px solid rgba(255, 255, 255, 0.12);
  border-radius: 24px;
  backdrop-filter: blur(10px);
  text-align: center;
  transition: all 0.3s;
  z-index: 1;
}

.timeline-card:hover {
  transform: translateY(-8px);
  border-color: rgba(255, 214, 0, 0.4);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3);
}

/* Icon Circle - PIÙ GRANDE */
.timeline-icon {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36px;
  margin: 0 auto 24px;
  box-shadow: 
    0 8px 24px rgba(255, 214, 0, 0.4),
    inset 0 -4px 8px rgba(0, 0, 0, 0.2);
  border: 4px solid rgba(0, 0, 50, 0.8);
}

.timeline-year {
  font-size: 40px;
  font-weight: 900;
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 8px;
  display: block;
}

.timeline-phase {
  font-size: 14px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: rgba(255, 214, 0, 0.8);
  margin-bottom: 20px;
}

.timeline-description {
  font-size: 15px;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.8);
}

@media (max-width: 968px) {
  .timeline-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }
  
  .timeline-grid::before {
    display: none;
  }
}

@media (max-width: 768px) {
  .storia-section {
    padding: 80px 24px;
  }
  
  .storia-title {
    font-size: 36px;
  }
  
  .timeline-card {
    padding: 40px 28px;
  }
  
  .timeline-icon {
    width: 70px;
    height: 70px;
    font-size: 32px;
  }
}