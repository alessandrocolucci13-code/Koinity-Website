Miglioramento Sezione "Come funziona"Obiettivo
Trasformare la sezione "Come funziona" da una semplice lista numerata in un box blu a un processo visivo step-by-step moderno, chiaro e coinvolgente che guidi i cinema attraverso il funzionamento della piattaforma.Problema Attuale

Box blu generico con lista troppo compatta
Numeri badge gialli piccoli e poco visibili
Mancanza di flow visivo tra gli step
Nessuna connessione visiva tra i passaggi
Layout statico che non comunica un "percorso"
Testo troppo lungo e poco scannable
Modifiche da implementare:1. Header Sezionecss.how-it-works-section {
  padding: 100px 40px;
  max-width: 1400px;
  margin: 0 auto;
  position: relative;
}

.section-header {
  text-align: center;
  margin-bottom: 80px;
}

.section-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 20px;
  background: rgba(255, 214, 0, 0.15);
  border: 2px solid rgba(255, 214, 0, 0.3);
  border-radius: 24px;
  font-size: 13px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: #FFD600;
  margin-bottom: 20px;
}

.section-title {
  font-size: 52px;
  font-weight: 900;
  color: #fff;
  margin-bottom: 16px;
}

.section-subtitle {
  font-size: 19px;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.7;
  max-width: 700px;
  margin: 0 auto;
}

@media (max-width: 768px) {
  .section-title {
    font-size: 36px;
  }
  
  .section-subtitle {
    font-size: 17px;
  }
}2. Timeline Verticale con Connettoricss.steps-container {
  position: relative;
  max-width: 1000px;
  margin: 0 auto;
}

/* Linea centrale connettore */
.timeline-line {
  position: absolute;
  left: 50%;
  top: 80px;
  bottom: 80px;
  width: 3px;
  background: linear-gradient(
    180deg,
    rgba(255, 214, 0, 0) 0%,
    rgba(255, 214, 0, 0.6) 20%,
    rgba(255, 214, 0, 0.6) 80%,
    rgba(255, 214, 0, 0) 100%
  );
  transform: translateX(-50%);
}

/* Progress bar animata (opzionale) */
.timeline-progress {
  position: absolute;
  left: 50%;
  top: 80px;
  width: 3px;
  height: 0%;
  background: linear-gradient(
    180deg,
    #FFD600 0%,
    #22C55E 100%
  );
  transform: translateX(-50%);
  transition: height 1s ease-out;
  box-shadow: 0 0 10px rgba(255, 214, 0, 0.6);
}

@media (max-width: 968px) {
  .timeline-line,
  .timeline-progress {
    left: 40px;
  }
}3. Step Card Design Alternato (Zigzag)css.step-item {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 40px;
  align-items: center;
  margin-bottom: 60px;
  position: relative;
  opacity: 0;
  animation: fadeInStep 0.8s ease-out forwards;
}

/* Alternating layout */
.step-item:nth-child(odd) .step-content {
  grid-column: 1;
  text-align: right;
}

.step-item:nth-child(odd) .step-number-container {
  grid-column: 2;
}

.step-item:nth-child(odd) .step-visual {
  grid-column: 3;
}

.step-item:nth-child(even) .step-visual {
  grid-column: 1;
}

.step-item:nth-child(even) .step-number-container {
  grid-column: 2;
}

.step-item:nth-child(even) .step-content {
  grid-column: 3;
  text-align: left;
}

/* Stagger animation */
.step-item:nth-child(1) { animation-delay: 0.1s; }
.step-item:nth-child(2) { animation-delay: 0.2s; }
.step-item:nth-child(3) { animation-delay: 0.3s; }
.step-item:nth-child(4) { animation-delay: 0.4s; }

@keyframes fadeInStep {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Mobile: Stack verticale */
@media (max-width: 968px) {
  .step-item {
    grid-template-columns: auto 1fr;
    gap: 24px;
    margin-bottom: 48px;
  }
  
  .step-item:nth-child(odd) .step-content,
  .step-item:nth-child(even) .step-content {
    grid-column: 2;
    text-align: left;
  }
  
  .step-item:nth-child(odd) .step-number-container,
  .step-item:nth-child(even) .step-number-container {
    grid-column: 1;
    grid-row: 1 / span 2;
  }
  
  .step-item:nth-child(odd) .step-visual,
  .step-item:nth-child(even) .step-visual {
    grid-column: 2;
    margin-top: 16px;
  }
}4. Number Container Grande e Distintivocss.step-number-container {
  position: relative;
  width: 100px;
  height: 100px;
  flex-shrink: 0;
  z-index: 2;
}

/* Circle background */
.step-number-bg {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 
    0 8px 32px rgba(255, 214, 0, 0.4),
    inset 0 -4px 8px rgba(0, 0, 0, 0.2);
  border: 4px solid rgba(0, 0, 50, 0.8);
  transition: all 0.4s;
  position: relative;
  overflow: hidden;
}

.step-item:hover .step-number-bg {
  transform: scale(1.1) rotate(10deg);
  box-shadow: 
    0 12px 40px rgba(255, 214, 0, 0.6),
    inset 0 -4px 8px rgba(0, 0, 0, 0.2);
}

/* Number */
.step-number {
  font-size: 42px;
  font-weight: 900;
  color: #000050;
  z-index: 2;
  position: relative;
}

/* Icon alternative (piccolo) */
.step-icon {
  position: absolute;
  bottom: -8px;
  right: -8px;
  width: 36px;
  height: 36px;
  background: rgba(34, 197, 94, 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  border: 3px solid rgba(0, 0, 50, 0.8);
  box-shadow: 0 4px 12px rgba(34, 197, 94, 0.4);
}

/* Ring pulsante */
.step-ring {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 120%;
  height: 120%;
  border: 3px solid rgba(255, 214, 0, 0.4);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  animation: pulse-step 2s ease-out infinite;
}

@keyframes pulse-step {
  0% {
    transform: translate(-50%, -50%) scale(1);
    opacity: 0.6;
  }
  100% {
    transform: translate(-50%, -50%) scale(1.3);
    opacity: 0;
  }
}5. Content Area con Typographycss.step-content {
  padding: 20px;
}

.step-title {
  font-size: 26px;
  font-weight: 800;
  color: #fff;
  margin-bottom: 12px;
  transition: color 0.3s;
}

.step-item:hover .step-title {
  color: #FFD600;
}

.step-description {
  font-size: 17px;
  line-height: 1.8;
  color: rgba(255, 255, 255, 0.75);
  transition: color 0.3s;
}

.step-item:hover .step-description {
  color: rgba(255, 255, 255, 0.9);
}

/* Additional info */
.step-info {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 16px;
  padding: 8px 16px;
  background: rgba(255, 214, 0, 0.1);
  border: 1px solid rgba(255, 214, 0, 0.2);
  border-radius: 12px;
  font-size: 13px;
  font-weight: 600;
  color: #FFD600;
}

.info-icon {
  font-size: 14px;
}6. Visual Element (Illustrazione/Icon)css.step-visual {
  padding: 32px;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.05) 0%,
    rgba(255, 214, 0, 0.03) 100%
  );
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 20px;
  backdrop-filter: blur(10px);
  transition: all 0.4s;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 180px;
}

.step-item:hover .step-visual {
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.08) 0%,
    rgba(255, 214, 0, 0.06) 100%
  );
  border-color: rgba(255, 214, 0, 0.3);
  transform: scale(1.02);
}

/* Visual icon/illustration */
.visual-icon {
  font-size: 80px;
  filter: drop-shadow(0 4px 16px rgba(255, 214, 0, 0.3));
  transition: transform 0.4s;
}

.step-item:hover .visual-icon {
  transform: scale(1.1) rotate(5deg);
}

/* Alternative: illustration container */
.visual-illustration {
  width: 100%;
  max-width: 200px;
  height: auto;
  opacity: 0.9;
  transition: opacity 0.3s;
}

.step-item:hover .visual-illustration {
  opacity: 1;
}7. Connector Arrows (Opzionale)css.step-connector {
  position: absolute;
  left: 50%;
  bottom: -40px;
  transform: translateX(-50%);
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(255, 214, 0, 0.6);
  font-size: 24px;
  animation: bounce-arrow 2s ease-in-out infinite;
}

@keyframes bounce-arrow {
  0%, 100% {
    transform: translateX(-50%) translateY(0);
  }
  50% {
    transform: translateX(-50%) translateY(8px);
  }
}

@media (max-width: 968px) {
  .step-connector {
    left: 40px;
    transform: translateX(0);
  }
}8. Duration/Time Badge (Opzionale)css.step-duration {
  position: absolute;
  top: 20px;
  left: 20px;
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: rgba(59, 130, 246, 0.2);
  border: 2px solid rgba(59, 130, 246, 0.3);
  border-radius: 20px;
  font-size: 12px;
  font-weight: 700;
  color: #3B82F6;
}

.duration-icon {
  font-size: 14px;
}9. Final CTA dopo gli stepcss.steps-cta {
  margin-top: 80px;
  text-align: center;
  padding: 60px 40px;
  background: linear-gradient(
    135deg,
    rgba(255, 214, 0, 0.1) 0%,
    rgba(34, 197, 94, 0.05) 100%
  );
  border: 2px solid rgba(255, 214, 0, 0.3);
  border-radius: 32px;
  backdrop-filter: blur(20px);
}

.cta-title {
  font-size: 32px;
  font-weight: 800;
  color: #fff;
  margin-bottom: 16px;
}

.cta-description {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.8);
  margin-bottom: 32px;
}

.cta-button {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  padding: 18px 40px;
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  border: none;
  border-radius: 16px;
  color: #000050;
  font-size: 18px;
  font-weight: 800;
  text-decoration: none;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
  box-shadow: 0 8px 24px rgba(255, 214, 0, 0.4);
}

.cta-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 32px rgba(255, 214, 0, 0.6);
}Struttura HTML Completahtml<section class="how-it-works-section">
  
  <!-- Header -->
  <div class="section-header">
    <span class="section-badge">
      <span>⚡</span>
      Processo Semplice
    </span>
    <h2 class="section-title">Come funziona</h2>
    <p class="section-subtitle">
      Quattro semplici passaggi per portare gli eventi più richiesti 
      nelle tue sale cinematografiche
    </p>
  </div>

  <!-- Timeline Container -->
  <div class="steps-container">
    
    <!-- Timeline Line -->
    <div class="timeline-line"></div>
    <div class="timeline-progress" id="timelineProgress"></div>
    
    <!-- Step 1 -->
    <div class="step-item">
      <div class="step-content">
        <h3 class="step-title">Monitora le proposte</h3>
        <p class="step-description">
          Accedi alla dashboard per vedere le proposte nella tua città 
          e i livelli di interesse. Dati in tempo reale sulla domanda locale.
        </p>
        <span class="step-info">
          <span class="info-icon">⏱️</span>
          Aggiornamento continuo
        </span>
      </div>
      
      <div class="step-number-container">
        <div class="step-ring"></div>
        <div class="step-number-bg">
          <span class="step-number">1</span>
        </div>
        <span class="step-icon">📊</span>
      </div>
      
      <div class="step-visual">
        <span class="visual-icon">📱</span>
        <!-- Oppure: <img src="/illustrations/step1.svg" class="visual-illustration"> -->
      </div>
      
      <div class="step-connector">↓</div>
    </div>

    <!-- Step 2 -->
    <div class="step-item">
      <div class="step-visual">
        <span class="visual-icon">✅</span>
      </div>
      
      <div class="step-number-container">
        <div class="step-ring"></div>
        <div class="step-number-bg">
          <span class="step-number">2</span>
        </div>
        <span class="step-icon">👍</span>
      </div>
      
      <div class="step-content">
        <h3 class="step-title">Valuta la domanda</h3>
        <p class="step-description">
          Quando una proposta raggiunge la soglia, ricevi una notifica 
          e puoi decidere se ospitare la proiezione. Pubblico garantito.
        </p>
        <span class="step-info">
          <span class="info-icon">🎯</span>
          Soglia minima: 50-100 prenotazioni
        </span>
      </div>
      
      <div class="step-connector">↓</div>
    </div>

    <!-- Step 3 -->
    <div class="step-item">
      <div class="step-content">
        <h3 class="step-title">Organizza l'evento</h3>
        <p class="step-description">
          Confermi date e orari, e la community riceve la notifica 
          per acquistare i biglietti. Coordinamento automatico senza sforzo.
        </p>
        <span class="step-info">
          <span class="info-icon">🗓️</span>
          Flessibilità totale su date/orari
        </span>
      </div>
      
      <div class="step-number-container">
        <div class="step-ring"></div>
        <div class="step-number-bg">
          <span class="step-number">3</span>
        </div>
        <span class="step-icon">📅</span>
      </div>
      
      <div class="step-visual">
        <span class="visual-icon">🎬</span>
      </div>
      
      <div class="step-connector">↓</div>
    </div>

    <!-- Step 4 -->
    <div class="step-item">
      <div class="step-visual">
        <span class="visual-icon">🎉</span>
      </div>
      
      <div class="step-number-container">
        <div class="step-ring"></div>
        <div class="step-number-bg">
          <span class="step-number">4</span>
        </div>
        <span class="step-icon">✨</span>
      </div>
      
      <div class="step-content">
        <h3 class="step-title">Sala piena</h3>
        <p class="step-description">
          Proietti i film con la certezza di avere un pubblico appassionato 
          e coinvolto. Zero rischio, massima soddisfazione.
        </p>
        <span class="step-info">
          <span class="info-icon">📈</span>
          95% tasso medio di riempimento
        </span>
      </div>
    </div>

  </div>

  <!-- Final CTA -->
  <div class="steps-cta">
    <h3 class="cta-title">Pronto a iniziare?</h3>
    <p class="cta-description">
      Richiedi una demo gratuita e scopri come Koinity può aiutare 
      il tuo cinema a riempire le sale
    </p>
    <a href="#demo" class="cta-button">
      <span>Richiedi Demo Gratuita</span>
      <span>→</span>
    </a>
  </div>

</section>JavaScript per Animazionijavascript// Timeline progress animation on scroll
function updateTimelineProgress() {
  const timeline = document.getElementById('timelineProgress');
  const stepsContainer = document.querySelector('.steps-container');
  
  if (!timeline || !stepsContainer) return;
  
  const containerRect = stepsContainer.getBoundingClientRect();
  const windowHeight = window.innerHeight;
  
  // Calculate progress
  const scrollProgress = Math.max(0, Math.min(1, 
    (windowHeight - containerRect.top) / (containerRect.height + windowHeight)
  ));
  
  timeline.style.height = `${scrollProgress * 100}%`;
}

window.addEventListener('scroll', updateTimelineProgress);
window.addEventListener('resize', updateTimelineProgress);

// Initial call
updateTimelineProgress();

// Intersection Observer per step animation
const stepsObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.style.animationPlayState = 'running';
    }
  });
}, {
  threshold: 0.2,
  rootMargin: '0px 0px -100px 0px'
});

document.querySelectorAll('.step-item').forEach(step => {
  step.style.animationPlayState = 'paused';
  stepsObserver.observe(step);
});

// Smooth scroll per CTA
document.querySelector('.steps-cta .cta-button')?.addEventListener('click', (e) => {
  e.preventDefault();
  document.querySelector('#demo')?.scrollIntoView({ 
    behavior: 'smooth' 
  });
});