STEP 4: Sezione Team - Card Migliorate
css/* === TEAM SECTION === */
.team-section {
  padding: 100px 40px;
  max-width: 1200px;
  margin: 0 auto;
}

.team-title {
  font-size: 48px;
  font-weight: 900;
  color: #fff;
  text-align: center;
  margin-bottom: 16px;
}

.team-subtitle {
  font-size: 18px;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.8);
  text-align: center;
  margin-bottom: 64px;
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
}

/* Team Grid */
.team-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 40px;
}

/* Team Card - MIGLIORATA */
.team-card {
  padding: 40px 32px;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.06) 0%,
    rgba(255, 214, 0, 0.03) 100%
  );
  border: 2px solid rgba(255, 255, 255, 0.12);
  border-radius: 24px;
  backdrop-filter: blur(10px);
  text-align: center;
  transition: all 0.3s;
}

.team-card:hover {
  transform: translateY(-8px);
  border-color: rgba(255, 214, 0, 0.4);
  box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3);
}

/* Avatar - PIÙ GRANDE E DISTINTIVO */
.team-avatar {
  width: 100px;
  height: 100px;
  background: linear-gradient(135deg, #FFD600, #FF8C00);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 44px;
  margin: 0 auto 24px;
  box-shadow: 
    0 8px 24px rgba(255, 214, 0, 0.4),
    inset 0 -4px 8px rgba(0, 0, 0, 0.2);
  border: 4px solid rgba(0, 0, 50, 0.8);
  position: relative;
  transition: all 0.3s;
}

.team-card:hover .team-avatar {
  transform: scale(1.05);
  box-shadow: 
    0 12px 32px rgba(255, 214, 0, 0.6),
    inset 0 -4px 8px rgba(0, 0, 0, 0.2);
}

/* Badge ruolo sull'avatar */
.team-badge {
  position: absolute;
  bottom: -8px;
  right: -8px;
  width: 32px;
  height: 32px;
  background: #22C55E;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  border: 3px solid rgba(0, 0, 50, 0.9);
  box-shadow: 0 4px 12px rgba(34, 197, 94, 0.4);
}

.team-name {
  font-size: 22px;
  font-weight: 800;
  color: #fff;
  margin-bottom: 8px;
}

.team-role {
  font-size: 15px;
  font-weight: 600;
  color: rgba(255, 214, 0, 0.9);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.team-description {
  font-size: 14px;
  line-height: 1.6;
  color: rgba(255, 255, 255, 0.7);
  font-style: italic;
}

@media (max-width: 968px) {
  .team-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }
}

@media (max-width: 768px) {
  .team-section {
    padding: 80px 24px;
  }
  
  .team-title {
    font-size: 36px;
  }
  
  .team-avatar {
    width: 90px;
    height: 90px;
    font-size: 40px;
  }
}
